source "http://rubygems.org"

# To load environment variables from .env
gem "dotenv", "~> 0.1.1"

# ActiveSupport dependency also requires i18n
gem "i18n", "~> 0.6.0"

# Sprinkle uses erubis for templates
gem "erubis", "~> 2.7.0"

# Sprinkle itself
gem "sprinkle", "~> 0.4.2"
